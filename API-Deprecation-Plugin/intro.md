# CKAD: API Deprecation and Deployment

Practice identifying and fixing deprecated Kubernetes API versions, then deploying the updated manifest.

In this scenario, you'll work with a manifest written for an older Kubernetes release — from before `apps/v1beta1` was removed in v1.16. Your cluster is running v1.36, where that API version no longer exists.

Click **Start Scenario** to begin.

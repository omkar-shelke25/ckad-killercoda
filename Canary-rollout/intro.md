# 🚀 Canary Rollout with nginx (namespace: default)

Create a **canary** Deployment that runs **nginx:1.20** alongside the existing Pods and handles **~20%** of the traffic.

Click **Start Scenario** to begin.

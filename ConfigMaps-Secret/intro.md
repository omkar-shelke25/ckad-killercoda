# Multiple ConfigMaps & Secret → Environment Variables (namespace: `api`)

Create **two ConfigMaps** and **one Secret** in the `api` namespace, inject them into a Pod as **environment variables**.

Click **Start Scenario** to begin.

# 🧩 Configure DNS and Traefik Ingress for Path-Based Routing

Click **Start** to begin the lab and create an Ingress for **streams.local** that routes traffic to two Services using **path-based routing**.

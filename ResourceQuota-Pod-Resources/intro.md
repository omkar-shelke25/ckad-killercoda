# CKAD: ResourceQuota + Requests/Limits

You will create a namespace, apply a ResourceQuota, and run a Deployment whose pod resources fit within that quota. The verifier checks both the quota and the pod resource settings.

Click **Start Scenario** to begin.

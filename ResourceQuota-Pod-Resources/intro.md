# CKAD: ResourceQuota + Requests/Limits

You will create a **namespace**, apply a **ResourceQuota**, and run a **Deployment** whose pod resources fit within that quota. The verifier will check both the quota and pod resource settings.

Click **Start Scenario** to begin.

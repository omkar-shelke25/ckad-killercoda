# 🐳 CKAD: Build Image and Save in OCI Format with Podman
     
## 📋 Your Mission
A simple Python Flask web application needs to be containerized using Podman and saved in OCI (Open Container Initiative) format for distribution to an environment without registry access.

All application files are already provided. You need to build the container image using Podman and export it in OCI format.

Click **🎬 Start Scenario** to begin your container image challenge!
